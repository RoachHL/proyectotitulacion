@model IEnumerable<sisetitulacion.Models.TBL_tipoUsuario>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombretitpousario)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombretitpousario)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id_TipoUsuario }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id_TipoUsuario }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id_TipoUsuario })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
