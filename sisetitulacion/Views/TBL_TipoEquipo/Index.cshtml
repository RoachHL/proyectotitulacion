@model IEnumerable<sisetitulacion.Models.TBL_TipoEquipo>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombreTipoEquipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.abreviatura)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombreTipoEquipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.abreviatura)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id_TipoEquipo }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id_TipoEquipo }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id_TipoEquipo })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
