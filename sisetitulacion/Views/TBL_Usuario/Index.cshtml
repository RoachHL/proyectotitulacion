@model IEnumerable<sisetitulacion.Models.TBL_Usuario>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TBL_tipoUsuario.nombretitpousario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nombresuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.contrasenha)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TBL_tipoUsuario.nombretitpousario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nombresuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.contrasenha)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id_usuario }) |
                @Html.ActionLink("Details", "Details", new { id=item.id_usuario }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id_usuario })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
